'use strict';

const uuid = require('uuid')
//require uuid to generate random user ids


module.exports = class User{
  constructor(req){

  }
}

//Randomly generated uuid import uuid
//generates on sign in and sign out

//take req.body.username and req.body.password, hash it, return the newly
//formatted user object.
